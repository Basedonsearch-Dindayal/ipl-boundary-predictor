import React from 'react';
import ReactDOM from 'react-dom/client';
import IPLBoundaryPredictor from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <IPLBoundaryPredictor />
  </React.StrictMode>,
);